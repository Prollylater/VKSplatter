#pragma once;

// Bucket List:

// Thread safetey first for when proper multi threading is introduced (locking strategies )
// Memory Fragmentation handling for released stuff
// Read on region bnased mem allocation, an dThread local storage

namespace cico
{
    namespace memory
    {
        enum class MemoryTag
        {

        };

    }
}
